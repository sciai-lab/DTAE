data:
  root: ~/data/endocrine_pancreas
device: cuda:0
git_rev: none
model:
  kwargs:
    activation: relu
    dims:
    - 3999
    - 2048
    - 256
    - 32
    - 2
    dropout: 0.0
  name: AE
optimizer:
  kwargs:
    lr: 0.0002
  name: Adam
random_seed : 16068
tensorboard:
  log_scalars_every: 10
training:
  batch_size: 256
  checkpoint_every: 50
  num_epochs: 300
