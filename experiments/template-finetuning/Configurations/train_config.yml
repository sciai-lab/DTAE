K: 50
weight_cosine: 15
weight_push_pull: 1
checkpoint:
  experiment: path/to/experiment/folder
  iteration: 45000
data:
  root: ~/data/example_dataset
device: cuda:3
git_rev: none
optimizer:
  kwargs:
    lr: 0.001
  name: Adam
random_seed : 0
tensorboard:
  log_scalars_every: 1
training:
  batch_size: 256
  checkpoint_every: 50
  num_epochs: 250
